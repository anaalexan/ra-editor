#include "Difference.h"

using namespace std;

shared_ptr<CRelation> CDifference::evaluate(vector<shared_ptr<CRelation>> & relations) {
    CRelation res;
    for(size_t i = 0; i < size(relations[0]->))
    


}